# Architecture Notes

This repository is intentionally organized to be easy to grow:

- `src/` contains implementation code.
- `tests/` contains automated tests.
- `.github/workflows/ci.yml` runs baseline quality checks.

As functionality is added, keep high-level design notes here.
